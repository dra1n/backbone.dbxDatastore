chai = require 'chai'
sinon = require 'sinon'

{DbxDatastore} = require '../src/backbone.dbxDatastore'

chai.should()

describe 'backbone.dbxDatastore', ->
  it 'should do something', ->
    true.should.equal true
